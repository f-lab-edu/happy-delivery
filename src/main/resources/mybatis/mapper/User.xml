<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.happy.delivery.infra.mybatis.UserMapper">
  <insert id="insert"  parameterType="com.happy.delivery.domain.user.User">
    INSERT INTO User (id, email, name, password, phoneNumber)
    VALUES (#{id}, #{email}, #{name}, #{password}, #{phoneNumber})
  </insert>

  <select id="findById"  resultType="com.happy.delivery.domain.user.User">
    SELECT id, email, name, password, phoneNumber FROM User WHERE id = #{id}
  </select>

  <select id="findByEmail"  resultType="com.happy.delivery.domain.user.User">
      SELECT id, email, name, password, phoneNumber FROM User WHERE email = #{email}
    </select>

  <delete id="deleteUser">
    DELETE FROM User WHERE id = #{id}
  </delete>

</mapper>